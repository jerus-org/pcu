[profile.default]
# Retry tests up to 2 times to surface flaky tests (e.g. env var races).
retries = 2

[profile.ci]
# In CI, fail fast on the first flaky detection pass; retries surface instability.
retries = 2
fail-fast = false
